import { TeamActions } from '../actions';

const initialState = { };

const team = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
};

export default team;
